import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container:{
    width: '100%',
    flex: 1,
    paddingHorizontal: 20,
  },
  content:{
    width: '100%',
    paddingTop: 135,
    paddingBottom: 184,
  }
})